<?xml version="1.0" encoding="utf-8"?>
<!-- This is the Flutter launch background. Kept minimal: solid white for light mode.
     The real custom splash is handled entirely in Dart (SplashScreen widget). -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@android:color/white"/>
</layer-list>
